int n = 10000000;

double pi = 0.0;
for (int i = 0; i < n; i += 1)
{
    if (i % 2 == 0)
    {
        pi += (1.0 / (i * 2 + 1));
    }
    else
    {
        pi -= (1.0 / (i * 2 + 1));
    }
}

echo(pi * 4);
