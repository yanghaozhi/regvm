#pragma once

//#include "vm.h"

class error
{
protected:
    template <typename T> void reg_info(struct regvm* vm, int i, T cb)
    {
        //cb(vm->reg.values[i].num, vm->reg.froms[i], vm->reg.types[i]);
    }

};
